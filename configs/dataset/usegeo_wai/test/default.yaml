dataset_str:
  "UseGeoWAI(
    split='${dataset.usegeo_wai.test.split}',
    resolution=${dataset.usegeo_wai.test.dataset_resolution},
    principal_point_centered=${dataset.usegeo_wai.test.principal_point_centered},
    seed=${dataset.usegeo_wai.test.seed},
    transform='${dataset.usegeo_wai.test.transform}',
    data_norm_type='${dataset.usegeo_wai.test.data_norm_type}',
    ROOT='${dataset.usegeo_wai.test.ROOT}',
    dataset_metadata_dir='${dataset.usegeo_wai.test.dataset_metadata_dir}',
    overfit_num_sets=${dataset.usegeo_wai.test.overfit_num_sets},
    variable_num_views=${dataset.usegeo_wai.test.variable_num_views},
    num_views=${dataset.usegeo_wai.test.num_views},
    covisibility_thres=${dataset.usegeo_wai.test.covisibility_thres},
    covisibility_thres_max=${dataset.usegeo_wai.test.covisibility_thres_max},
    sampling_mode='${dataset.usegeo_wai.test.sampling_mode}',
    load_modalities=${dataset.usegeo_wai.test.load_modalities},)"
split: 'test'
dataset_resolution: ${dataset.resolution_test_usegeo}
principal_point_centered: ${dataset.principal_point_centered}
seed: 777
transform: 'imgnorm'
data_norm_type: ${model.data_norm_type}
ROOT: ${root_data_dir}/usegeo
dataset_metadata_dir: ${mapanything_dataset_metadata_dir}
overfit_num_sets: null
variable_num_views: ${dataset.test.variable_num_views}
num_views: ${dataset.num_views}
covisibility_thres: ${dataset.covisibility_thres_min}
covisibility_thres_max: ${dataset.covisibility_thres_max}
sampling_mode: ${dataset.sampling_mode}
load_modalities: ["image", "depth"]
